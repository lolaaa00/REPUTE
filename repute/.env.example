# Repute — Environment Configuration
# Copy to .env.local and fill in values after deployment.
# NEVER commit .env.local to version control.

# Deployed contract addresses on GenLayer Studionet (chain 61999)
NEXT_PUBLIC_PROFILE_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
NEXT_PUBLIC_VAULT_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# Deployment: set via CI environment, never committed
# REPUTE_DEPLOYER_PRIVATE_KEY=0x...
